<%= form_for [@gallery, @painting] do |f| %>
<% if @painting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@painting.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% @painting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :artist %>
    <%= f.text_field :artist %>
  </div>


  <div>
    <%= f.submit(value="Submit the cool form for a new rad art painting") %>
  </div>

<% end %>
